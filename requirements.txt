requirements
실행 환경
Tkinter를 사용하므로 Colab에서는 실행 불가합니다. Windows Python 기본 런타임이나 idle을 이용하여야 합니다.

라이브러리
$ pip install rapidfuzz

코드 수정
CSV 파일의 절대 경로가 하드코딩 되어있습니다. 상대 경로 사용시 오류가 발생합니다. 코드와 데이터 파일을 다운받은 후 def load_books()의 with open(...) 부분을 raw 문자열 형태의 CSV 파일 절대 경로로 다시 입력해야 합니다.